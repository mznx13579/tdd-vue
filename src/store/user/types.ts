export interface DecodeToken {
  access_ip: string;
  o: number;
  o_name: string;
  type: string;
  u: number;
  u_config: number;
  u_id: string;
  u_lang: string;
  u_level: string;
  u_name: string;
  u_tz: string;
}
